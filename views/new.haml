#shorturl
  %a(href="#{locals[:link]}") #{locals[:link]}
  #qrcode
    %table
      - qr.modules.each_index do |x|
        %tr 
        - qr.modules.each_index do |y|
          - if qr.is_dark(x,y)
            %td(class="black")
          - else
            %td(class="white")
